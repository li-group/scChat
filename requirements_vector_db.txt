# Vector Database Dependencies for Enhanced Conversation History
# Install with: pip install -r requirements_vector_db.txt

# Core vector database and embeddings
langchain-huggingface>=0.0.3
langchain-chroma>=0.1.0
chromadb>=0.4.22
sentence-transformers>=2.2.2

# For embedding models and security
transformers>=4.36.0
torch>=2.6.0  # Required for security fixes in torch.load

# Fallback requirements (if above fail)
# The system will work with basic functionality even if these fail to install
# Alternative: pip install torch==0.28 (older version without security restrictions)

# Note: Uses sentence-transformers/all-MiniLM-L6-v2 model (reliable and fast)
# Alternative bio-specific model: pritamdeka/BioBERT-mnli-snli-scinli-scitail-mednli-stsb